---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Values.guid }}-{{ .Values.tenant }}-registry-auth-role
rules:
- apiGroups: [""]
  resources:
  - configmaps
  verbs:
  - get
  - list
  - create
  - patch
  - update