---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: allow-read-pull-secret
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["pull-secret"]
    verbs: ["get"]