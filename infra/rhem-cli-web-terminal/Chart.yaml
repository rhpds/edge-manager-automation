---
apiVersion: v2
name: rhem-cli-web-terminal
description: >
  Builds a new web console image from the current one with the flightctl CLI, then updates the image used by the web console terminal
type: application
version: 1.0.0
appVersion: "1.0.0"